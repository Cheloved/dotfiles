#alias sd = "cd $(find -P ./ -type d | fzf)"

# function sd
#     cd $(find -P ./ -type d | fzf)
# end
# funcsave sd 

if status is-interactive
    # Commands to run in interactive sessions can go here
end
